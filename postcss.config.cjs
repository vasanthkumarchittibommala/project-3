// Local PostCSS configuration for this project.
// This overrides any global PostCSS config (for example in your user folder)
// that might be trying to load Tailwind as a plugin and causing errors.
//
// If you later decide to add Tailwind, replace the empty plugins object with:
//   const tailwind = require('@tailwindcss/postcss')
//   module.exports = { plugins: { tailwind } }

module.exports = {
  plugins: {
    // No PostCSS plugins are required for this project right now.
  },
}

